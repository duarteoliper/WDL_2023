# Add the working directory to `PYTHONPATH`; allows Jupyter notebooks in the `notebooks` folder to import from project dir
export PYTHONPATH="$PYTHONPATH:$(pwd)"

# Activate environment
eval "$(conda shell.bash hook)"
conda activate wdl_2023